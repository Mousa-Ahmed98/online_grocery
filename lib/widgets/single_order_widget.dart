import 'package:flutter/material.dart';

class SingleOrderWidget extends StatelessWidget {
  const SingleOrderWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
    );
  }
}
